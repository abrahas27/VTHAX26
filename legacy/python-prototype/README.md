# Python prototype (superseded)

Early stubs from before the spec settled on Next.js Route Handlers + the Vercel AI SDK.
Kept for reference only; not part of the build, lint, or typecheck. The live equivalents will be
`src/lib/db/lakebase.ts` (connection), `src/lib/db/queries.ts` (profile reads) and
`src/lib/agent/tools.ts` (agent tools). Safe to delete once the team agrees.
