// Placeholder: the For You dashboard (F4) is built in P2.
export const dynamic = "force-dynamic";

export default function Dashboard() {
  return (
    <main className="mx-auto flex w-full max-w-2xl flex-1 flex-col justify-center gap-3 p-8">
      <h1 className="text-xl">Dashboard</h1>
      <p className="text-muted-foreground text-sm">
        Events, recruiter visits, clubs, and your roadmap arrive in phase P2.
      </p>
    </main>
  );
}
